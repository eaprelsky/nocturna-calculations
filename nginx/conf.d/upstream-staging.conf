# Upstream configuration - STAGING environment

upstream nocturna_backend {
    server nocturna-staging-api:8000 max_fails=3 fail_timeout=30s;
    
    # Connection pool settings
    keepalive 16;
    keepalive_requests 100;
    keepalive_timeout 60s;
}
